package de.htwberlin.webtech.Fitnessplan.persistence;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TraindayRepository extends JpaRepository<TraindayEntity, Long> {
}
